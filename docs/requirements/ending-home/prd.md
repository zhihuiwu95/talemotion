# REQ-ENDING-HOME：结束后回到首页

2026-09-13；引用 prd / design / arch v1.0；代码基线 0633508，开始时工作区干净。

问题：结束页原有“回故事小屋”是家长样式的小字按钮，儿童不容易发现和点击。
范围：所有故事包结尾、第一版手套故事两个结尾、手套配对与三个活动的完成/告别页面。

- AC-01：结束操作区首个按钮统一为“回到首页”，房子图标配文字，深绿底白字，24px 字号、至少 68px 高；窄屏不溢出。
- AC-02：点击复用 onHome 返回故事小屋；清除 story URL 参数，播放器卸载并清理声音和计时器。
- AC-03：保留重玩、线下陪玩和家长记录操作；首页按钮无需先完成观察表。

设计与架构：共享 EndingHomeButton 及独立样式，四个播放器复用。遵循 DES-04/05/12/13/14、ARCH-03/05/17/21/22。无新增依赖、台词、数据契约或存储；无基线偏离。通过既有路径测试验证结尾返回，运行 quality；320px 目标结束页做浏览器检查，真实设备触控和亲子体验单列待验证。

验证：
- `npm run quality` 通过：16 个测试文件、69 个 Vitest 测试、15 个 Python 测试；场景/故事/音频校验、lint、构建均通过。
- 既有路径测试补充首页按钮回调断言，覆盖四篇故事的全部 acceptance 路径、第一版手套两个结尾和四个活动。
- 浏览器 320 × 740：野餐活动完成页按钮实测 243 × 68px，花园故事花边结尾 239 × 68px；页面无横向溢出，故事长页可滚动访问按钮。两处点击均返回首页且 URL 移除 story 参数。
- 浏览器检查使用静音；真实手机触控、声音与儿童发现性仍待验证。历史播放器的视觉检查本次未逐一重复。
- `git diff --check` 通过；无台词和音频变更。提交、推送、部署未执行。
